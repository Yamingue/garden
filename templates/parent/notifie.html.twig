{% extends "base.html.twig" %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-10 col-md-6 col-lg-4 mx-auto">
                <div class="card mt-2 border mb-2">
                    <div class="card-body">
                        {{form_start(form)}}
                        {{form_widget(form)}}
                        <button type="submit" class="btn btn-success mt-3"> Save <i class="fas fa-save ms-1"></i></button>
                        {{form_end(form)}}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}