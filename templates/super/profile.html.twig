{% extends "super.html.twig" %}

{% block body %}
<div class="grid-margin stretch-card">
    <div class="card">
        <div class="card-header">
            Change information
        </div>
        <div class="card-body">
            {{form(form)}}
        </div>
    </div>
</div>

<div class="grid-margin stretch-card">
    <div class="card">
        <div class="card-header">
            Change Password
        </div>
        <div class="card-body">
            {{ form(passform) }}
        </div>
    </div>
</div>
{% endblock %}