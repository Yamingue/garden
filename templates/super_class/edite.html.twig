{% extends "super/index.html.twig" %}

{% block title %}Update class{% endblock %}

{% block body %}
<div class="container mt-2">
    <div class="row">
        <div class="card col-sm-6  mx-auto">
            <div class="card-body">
                {{ form_start(form)}}
                {{ form_widget(form)}}
                <button type="submit" class="btn btn-gradient-primary btn-sm" role="button">
                    <i class="fa fa-refresh me-2"></i>
                    Update
                </button>
                {{ form_end(form)}}
            </div>
        </div>
    </div>
</div>
{% endblock %}