{% extends "super.html.twig" %}
{% block body %}
{{ dump() }}
<div class="container">
    <div class="row">
        <div class="col-sm-10 mx-auto">
            <div class="card mt-2 border mb-2">
                <div class="card-header">
                    {{name}}
                </div>
                <div class="card-body">
                    {{form_start(form)}}
                    {{form_widget(form)}}
                    <button type="submit" class="btn btn-success"> Ajouter <i class="fas fa-plus ms-1"></i></button>
                    {{form_end(form)}}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}